<section class="section" style="text-align:center;padding:var(--space-section) var(--space-md);">
  <div class="container" style="max-width:600px;">
    <p style="font-size:5rem;margin-bottom:var(--space-sm);">🪞</p>
    <h1 class="heading-lg" style="margin-bottom:var(--space-sm);">Page introuvable</h1>
    <p style="color:var(--color-text-muted);margin-bottom:var(--space-md);">
      Cette page n'existe pas ou a été déplacée.
    </p>
    <a href="{{ routes.root_url }}" class="btn btn--accent">
      Retour à l'accueil
    </a>
  </div>
</section>
